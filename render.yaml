services:
  - type: web
    name: whatsapp-chatbot
    env: java
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/*.jar
    envVars:
      - key: JAVA_OPTS
        value: -Xmx512m
